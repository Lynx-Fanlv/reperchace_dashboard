# 预计购药分析看板 · 交付文档

> 适用版本：**v2026-09-24**（新增：快照可按品种范围导出；快照不再含整体小结与脱敏开关；
> 医院/科室/医生 改为严格取末次购药记录的值）
> 文档定位：**新同事拿到项目后，可以只看这一份**——既能上手日常使用，也能接手维护与二次开发。

---

## 0. 接手须知（先看这一节）

**你拿到的是一个独立副本。** 这个包里**不含 `.git` 目录**，因此：

- 没有指向原团队的远端仓库，**你怎么改、怎么提交，都不会影响原团队那边**；
- 同理，原团队后续的更新也不会自动流到你这里——需要同步要主动找他们要新包。
- 你可以自由地 `git init` 建立自己的仓库，历史从零开始。

**环境要求**

| 你要做什么 | 需要什么 |
|---|---|
| 只是用看板（上传表格、导出） | **只需要浏览器**。直接双击 `index.single.html` 即可，无需安装任何东西 |
| 改代码并重新构建 | **Python 3.7+**（跑 `build.py`） |
| 跑测试 | **Node.js 18+**（测试脚本用 Node 跑） |

依赖已经**全部内置在 `vendor/` 里**，不需要 `npm install`，也没有 `package.json`。

**第一步建议按这个顺序做**

1. 双击 `index.single.html`，随便找三张表试试，先建立手感；
2. 读本文第 5 节【核心计算口径】——**这是全项目的心脏**，改代码前必须先看懂它；
3. 按你的业务需求调参：品种与周期在 `mapping.js` 的 `PRODUCT_FAMILIES`，报表列名识别在 `KEYWORD_RULES`；
4. 改完跑 `python build.py && python build.py --single` 重新生成产物，再跑一遍测试。

**几个必须知道的坑**

- `index.html` 与 `index.single.html` 是**构建产物**，不要手改；改界面改 `index.template.html`，改逻辑改三个 `.js`。
- 测试脚本默认从**当前用户的 `Downloads` 目录**读样例 Excel；放在别处就设环境变量：
  ```bash
  SAMPLE_DIR="D:/my_sample_data" node tests/_test_e2e.js
  ```
  （找不到样例文件时相关测试会自动跳过，不影响其余测试）
- **`README.md` 的部分内容已过时**（应购药日期公式、周日历口径、导出按钮、Pages 分支等），
  **以本文件为准**。收到原团队更新时留意这一点。
- 文中的线上地址 `https://lynx-fanlv.github.io/reperchace_dashboard/` 与仓库地址属于**原团队**，
  **不要往那个仓库推送**；你要发布自己的版本请按 [12.2 自己部署一份](#122-自己部署一份无仓库权限时) 操作。

⚠️ **数据合规**：本项目全程在浏览器内计算、数据不出本机。但**导出名单（不脱敏字段）与随访回传表含明文个人信息**，
仅可在团队内部流转——换到新团队同样适用，详见第 9 节。

---

## 目录

0. [接手须知（先看这一节）](#0-接手须知先看这一节)
1. [5 分钟上手](#1-5-分钟上手)
2. [项目定位与解决的问题](#2-项目定位与解决的问题)
3. [交付物清单](#3-交付物清单)
4. [数据要求（三张表）](#4-数据要求三张表)
5. [核心计算口径（最重要）](#5-核心计算口径最重要)
6. [界面操作手册](#6-界面操作手册)
7. [导出与快照](#7-导出与快照)
8. [随访回传闭环](#8-随访回传闭环)
9. [隐私与合规](#9-隐私与合规)
10. [维护与二次开发](#10-维护与二次开发)
11. [测试](#11-测试)
12. [发布部署](#12-发布部署)
13. [已知限制与注意事项](#13-已知限制与注意事项)
14. [排错 FAQ](#14-排错-faq)
15. [附录 A：字段映射关键字全表](#附录-a字段映射关键字全表)
16. [附录 B：术语表](#附录-b术语表)

---

## 1. 5 分钟上手

**打开方式（任选其一）**

| 方式 | 操作 | 适用场景 |
|---|---|---|
| 在线 | 浏览器打开 https://lynx-fanlv.github.io/reperchace_dashboard/ | 日常使用，始终是最新版 |
| 单文件 | 双击 `index.single.html` | 离线/无网环境；也是发给同事的最简方式（约 2 MB，库已内联） |
| 本地源码 | `python -m http.server 8000` 后访问 `http://127.0.0.1:8000` | 二次开发调试 |

**三个动作就能出结果**

1. **拖入三张表**（销售明细 + 随访任务 + 患者用药周期；后两张可选），系统按表头自动识别类型。
2. **看状态卡片**：本周应回购多少人、其中应回未回多少、已逾期多少。
3. **筛选 + 导出**：按品种/医院/药房/随访人筛出要打的名单 → 点「导出名单」出 Excel。

> 全程纯前端计算，**数据不会离开本机**（无后端、无接口、无埋点）。

---

## 2. 项目定位与解决的问题

给医药随访团队用的 **回购预测看板**。输入是常规业务报表，输出是"**这周该催谁、为什么、催了没**"。

它替人做掉的三件体力活：

1. **算应购药日期** —— 从销售明细自动算出每个患者每个品种下次该买的日期，不用人工拿计算器按周期推。
2. **匹配随访记录** —— 自动找到对应那次随访的原始内容（用药状态、停药原因、随访小结），不用在两张表之间来回 V 找。
3. **判定状态 + 生成话术** —— 把患者分成「应回购（应回未回 / 应回已回）/ 已逾期 / 未到期」，并生成可直接复制到群里的整体小结。

额外能力：**展示层脱敏**（投屏、共享屏幕时一键隐去姓名电话）、**脱敏快照**（生成不含个人信息的只读网页外发）、**随访回传**（在看板里填的备注，下次分析自动接续）。

技术形态：**单页 HTML + 纯前端**。逻辑全在浏览器里跑，没有服务器，因此部署成本为零、也不存在数据外泄的服务端环节。

---

## 3. 交付物清单

### 3.1 文件

```
repurchase_dashboard/
├── index.html            # 【产物·勿手改】Web 部署版：逻辑已内联，vendor 库走外链
├── index.single.html     # 【产物·勿手改】单文件版：连 vendor 库也内联，可离线双击
├── index.template.html   # 【源】HTML 结构 + 全部 CSS —— 改界面改这里
├── app.js                # 【源】合并计算 / 状态判定 / 筛选 / 渲染 / 导出 / 快照 / 回传
├── pipeline.js           # 【源】Excel 解析与归一化（三表识别、列映射、脱敏）
├── mapping.js            # 【源】字段关键字映射 / 品种归一化 / 随访信号与原因分类规则
├── build.py              # 构建脚本：把三个 js 内联进模板，产出上面两个 html
├── vendor/
│   ├── xlsx.full.min.js  # SheetJS —— 浏览器内解析 Excel
│   └── exceljs.min.js    # ExcelJS —— 生成带样式的导出 Excel
├── tests/                # Node 验证脚本（19 个，见第 11 节）
└── README.md             # 简版说明（⚠️ 部分内容已过时，见文末提示）
```

### 3.2 代码分工（改哪里找哪里）

| 想改什么 | 改哪个文件 |
|---|---|
| 页面结构、样式、按钮 | `index.template.html` |
| 判定逻辑、列表渲染、导出内容、快照 | `app.js` |
| Excel 列名识别、新表头兼容、脱敏算法 | `pipeline.js` + `mapping.js` |
| 品种周期默认值、随访信号关键词、原因分类 | `mapping.js` |

### 3.3 仓库与分支

| 分支 | 内容 | 用途 |
|---|---|---|
| `main` | 源码 + 构建产物（`index.html`、`index.single.html`） | 开发主线 |
| `gh-pages` | **只有** 单文件版，重命名为 `index.html` | GitHub Pages 发布源 |

仓库地址：`https://github.com/Lynx-Fanlv/reperchace_dashboard.git`

---

## 4. 数据要求（三张表）

### 4.1 总览

| 表 | 是否必需 | 作用 |
|---|---|---|
| **销售明细** | **必需** | 唯一的购药事实来源。没有它整个看板无数据 |
| **随访任务** | 可选 | 提供随访内容与信号（停药/推迟/正常）；缺省则随访相关列空白，状态仍可判定 |
| **患者用药周期表** | 可选 | 按患者覆盖周期；缺省则用品种标准周期 |

**三张表可以一次全拖进去**，系统逐 sheet 按表头自动识别类型。不要求文件名固定，也不要求 sheet 名固定。

### 4.2 销售明细（必需）

| 列（含容错写法） | 必需 | 说明 |
|---|---|---|
| 销售时间 / 购药时间 / 成交时间 | ✅ | 每行一次购药；应购药日期由此推导 |
| 商品名称 / 药品名称 / 产品名称 / 产品 / 药品 | ✅ | 归一到品种家族（见 4.4） |
| 会员姓名 / 客户姓名 / 患者姓名 / 姓名 | ✅ | 患者身份主键之一 |
| 会员电话 / 手机号 / 联系电话 / 手机 / 电话 | 强烈建议 | **患者匹配优先用电话**；缺失时退化为按姓名匹配 |
| **销售数量 / 数量** | 建议 | **购药数量**，用于「一次买多个周期」的计算（见 5.2）。缺失时按 1 处理 |
| 会员号 / 会员编号 / 会员卡号 | 建议 | 「仅会员号」脱敏方式需要它 |
| **医疗单位** | 建议 | **医院列唯一的取值来源**。只精确匹配「医疗单位」这一列名；底表若无此列，医院列留空 |
| 药房名称 / 门店 / 药店名称 / 药店 / 药房 | 建议 | 筛选维度 |
| 处方医生 / 开单医生 / 医生 | 建议 | 名单展示的医生列 |
| 处方科室 / 科室 / 就诊科室 | 可选 | 名单展示 |
| 适应症 / 临床诊断 / 诊断 | 可选 | |
| 年龄 / 性别 | 可选 | |

> **医院 / 科室 / 医生 的取值口径（2026-09-24 定）**
>
> 三个字段都取**该患者该品种「销售时间最末次」那条记录**的值，不取文件末行
> （销售明细常按销售时间**降序**导出，末行反而是最早那次购药）。
>
> - **末次那条为空 → 该字段就显示为空**，不会回退到更早那次的非空值。
>   这样「界面显示的值 ≡ 末次记录的值」，可以直接拿销售明细核对，不会出现
>   「末次明明是 X，界面却是 Y」的疑问。
> - 同一天有多条（一笔购药被拆成多行）：用该日内**最后一个非空值**补齐，与文件行序无关。
> - **药房筛选生效时**，只在该药房的购药记录里取末次（与该筛选下其他列口径一致）。
>   因此「选了 A 药房」时，医生/科室可能是 A 药房那次的值，不等于患者在其他药房的末次。
>
> 排查工具：`node tests/_diag_doctor_dept.js` —— 拿真实销售明细核对
> 「界面值是否严格等于末次记录值」，并打印实际选中的列（医院=医疗单位 / 医生=处方医生 / 科室=处方科室）。
> 可指定文件：`set SALES_FILE=销售明细查询报表 (16).xlsx`。

### 4.3 随访任务（可选）

| 列（含容错写法） | 说明 |
|---|---|
| 任务状态 | 用于区分「已完成」与未执行任务 |
| 患者 / 患者姓名 / 姓名 | 患者身份（匹配兜底） |
| 联系电话 / 手机号 / 电话 / 电话号码 | 患者匹配首选 |
| 计划执行时间 / 计划执行日期 | 无执行时间时作为随访日期 |
| 执行时间 | **优先作为随访日期** |
| 产品 / 药品 / 商品 | 用于"同品种优先"匹配 |
| 服务摘要 / 任务摘要 / 随访类型 / 任务类型 | 随访类型展示 |
| 执行人 / 随访人 / 经办人 / 负责人 / 坐席 / 话务员 / 客服 | 筛选维度「随访人」 |
| 随访小结 / 小结 / 随访记录 / 患者反馈内容 / 患者反馈 / 备注 | 自由文本，参与信号与原因判定 |
| 取消原因 | |
| 患者目前的用药状态 | 信号判定 |
| 是否已按时购药 / 已按时购药 / 用药周期状态 | 信号判定 |
| 是否判定患者属于易脱落 / 是否脱落 / 是否确认脱落 | 信号判定 |
| 判断为易脱落的原因 / 脱落原因 / 脱落·流失原因 | 原因分类 |
| 推迟用药的原因 / 延迟用药的具体原因 / 未按计划持续用药原因 | 原因分类 |
| 停药的原因是什么 / 停药的具体原因 / 停止用药，主要是谁的建议 | 原因分类 |
| 还在按照处方继续使用 ××× / 患者近一次用药情况 / 剂量类列 | 信号判定 |

> **多模板合并大表**可以直接用：同一个逻辑字段在不同模板里列名不同，系统会**把所有命中的列都收集起来**，逐行取有值的那一列（见附录 A 的多值字段）。

### 4.4 患者用药周期表（可选）

| 列 | 说明 |
|---|---|
| 患者 / 患者姓名 / 姓名 | 患者标识 |
| 用药周期 / 周期天数 / 购药周期 / 周期 | 天数，正整数 |

**周期取值优先级**：`患者用药周期表（按姓名）` → `品种标准周期` → `30 天`

**品种标准周期**：内置 百泽安 21 天 / 百悦泽 28 天 / 索托克拉 28 天；**只按上传数据中实际出现的品种生成**，出现的新品种默认 30 天，界面可逐个修改。

**品种家族归一**：商品名含以下关键词即归入对应家族——

| 家族 | 关键词 | 标准周期 |
|---|---|---|
| 百泽安 | 替雷利珠 / 百泽安 / tislelizumab | 21 天 |
| 百悦泽 | 泽布替尼 / 百悦泽 / zanubrutinib | 28 天 |
| 索托克拉 | 索托克拉 / sotoclax | 28 天 |

未匹配的名称原样截断展示（不丢弃）。

---

## 5. 核心计算口径（最重要）

> 这一节是看板的"心脏"。**所有数字都从这几条规则来**，排查问题时先回到这里。

### 5.1 第一步：得到每个患者×品种的购药记录

按 **电话数字** 聚合（无电话则按姓名），每个患者每个品种得到一串购药记录：

```
[{日期: 2026-08-01, 数量: 3}, {日期: 2026-08-20, 数量: 1}, ...]
```

- 购药数量做容错解析：`2盒` → 2、`1.5` → 1.5、`1,200` → 1200；
  **缺失、非数字、非正数一律按 1**（等价于"不放大"，与历史行为一致）。
- **同一天的多行数量相加**（3 行各买 1 支 = 一次买了 3 支）。

### 5.2 第二步：算出「本次购药覆盖天数」

```
覆盖天数 = 购药数量 × 单次用药周期        （该品种勾选「按数量」时）
覆盖天数 = 单次用药周期                   （未勾选时，数量按 1 计）
```

「按数量」是**每个品种一个开关，默认关闭**。所以：
**默认状态下，本功能不影响任何历史结果**——想让哪个品种"一次买几个周期就按几个周期算"，把它的「按数量」勾上。

### 5.3 第三步：按所选「周期算法」推进「库存耗尽日」

三条记录按日期升序逐条推进，`库存耗尽日` 就是**下次应购日**：

```
剩余库存 = 上次库存耗尽日 - 本次购药日

重置式     base = 本次购药日
累计式     base = max(本次购药日, 上次库存耗尽日)
累计+容差  base = 剩余库存 ≤ 容差天数 ? 本次购药日 : 上次库存耗尽日

库存耗尽日 = base + 本次覆盖天数
```

| 算法 | 语义 | 什么时候用 |
|---|---|---|
| **重置式**（默认） | 每次购药都从本次重新起算 | 最直观、最好解释 |
| **累计式** | 只要还有剩余库存就往后顺延 | 患者习惯囤药 |
| **累计 + 容差** | 剩余库存 > 容差算囤药（顺延）；≤ 容差算小幅提前（重新起算） | 兼顾两者，避免小幅提前累积导致预测日期长期漂移 |

**容差天数默认 7 天**，仅在「累计+容差」下可改。

**关键性质**：药已吃完才买（剩余库存为负）时，三种算法**都自动走重置分支**——不需要额外判断。

**实例（周期 21 天）**：8/1 买 3 个周期、8/20 再买 1 个周期

| 算法 | 8/1 的覆盖 | 8/20 的处理 | 下次应购日 |
|---|---|---|---|
| 重置式 | — | 从 8/20 重新起算 21 天 | **9/10** |
| 累计式 | 覆盖到 10/3 | 剩余 44 天 > 0，顺延 21 天 | **10/24** |
| 累计+容差(7) | 覆盖到 10/3 | 剩余 44 天 > 7 → 顺延 | **10/24** |
| 累计+容差(60) | 覆盖到 10/3 | 剩余 44 天 ≤ 60 → 重置 | **9/10** |

### 5.4 「应购药日期」与「预判应购日」

- **应购药日期** = 推进到**全部**购药记录后的库存耗尽日 = **下次应该买的日期**
- **预判应购日** = 推进到「**除最后一次以外**」记录的库存耗尽日 = **上一次本该买的日期**

「预判应购日」用于对比实际购药是提前还是延后。**仅当该患者该品种有 ≥2 条购药记录时才有值**（只有 1 条时无参照，显示 `—`）。

### 5.5 随访匹配规则

1. 先按**电话数字**找该患者的随访；找不到再按**姓名**。
2. 优先取**同品种**的随访；同品种一条都没有时，用该患者的全部随访。
3. 优先「**真实执行过**」的随访（有执行时间，或任务状态含「已完成」），避免未执行的空任务干扰。
4. 在上一步的结果里，取**应购药日期之前、最近的一次**；若之前一条都没有，取**之后最早的一条**。
5. 随访日期 = `执行时间` 的日期部分，缺失时用 `计划执行时间`。

### 5.6 状态判定（周维度 · 购药事实优先）

时间窗口 = **所选周**（上周 / 本周 / 下周，默认本周），其起止由「**参考日**」和「**周截止日**」共同决定（见 5.7）。

判定顺序（**从上往下，命中即停**）：

| 顺序 | 条件 | 结果 |
|---|---|---|
| 1 | **所选周内有购药记录** | **应回购 · 应回已回** |
| 2 | 应购药日期 < 周首 | **已逾期** |
| 3 | 应购药日期 ≤ 周末 | **应回购 · 应回未回** |
| 4 | 其余 | **未到期** |

- **应回购（总集）= 应回未回 + 应回已回**
- 第 1 条最优先：只要当周有购药记录就算「已回」，**包括提前囤药的情况**
- 应购药日期落在所选周的行会**置顶显示 + 浅金色底色**，导出 Excel 同步整行浅金底

### 5.7 时间口径：参考日 + 周截止日

- **参考日**：默认今天，可手动改为历史日期以回看当时的状态（回看历史周）。
- **周截止日**：定义"本周"在哪一天结束，默认**周日**（= 自然周 周一~周日）。

| 周截止日选择 | 得到的"本周" |
|---|---|
| 周日（自然周） | 周一 ~ 周日 |
| 周五 | **上周六 ~ 本周五** |
| 周六 | 周日 ~ 周六 |
| 周一 / 周二 / 周三 / 周四 | 周二~周一 / 周三~周二 / … |

算法：本周 = `[截止日-6, 截止日]`，截止日取「**参考日当天或之后**最近的该星期几」，因此**本周必定包含参考日**。上周 / 下周各平移 7 天。

---

## 6. 界面操作手册

页面从上到下分五区：

### 6.1 上传区

- 整条可点击选文件，也可拖拽；支持一次多选。
- 每个文件会显示**自动识别出的类型**（销售 / 随访 / 周期 / 未识别）。
- **未识别**的文件会在点「开始分析」时被拦下并列出文件名，移除后重试即可。
- 分析完成后上传区仍保留，可增删文件后再次分析。
- 顶部会显示：`销售明细 N 条 · 随访任务 M 条 · 用药周期 K 人`（若本轮有回传数据接续，会追加提示）。

### 6.2 判定配置栏（决定"怎么算"）

| 控件 | 作用 |
|---|---|
| 时间：上周 / 本周 / 下周 | 状态判定的时间窗口 |
| 参考日 | 时间基准，改成历史日期即可回看历史周 |
| **周截止日** | 定义"本周"的起止（见 5.7） |
| 右侧范围标签 | 实时显示所选周的**实际日期区间与截止星期** |
| **周期算法** | 重置式 / 累计式 / 累计+容差（见 5.3） |
| **容差** | 天数，仅在「累计+容差」下可改 |
| 标准周期 | 每个品种一个天数输入框 + **「按数量」勾选框**（勾选后数量×周期，见 5.2） |

### 6.3 筛选区

- **状态卡片**：应回购 / 未到期 / 已逾期（+ 应回购细分 chips）。点选即筛选；**卡片计数会随其他筛选实时变化**。
- **未购药原因 chips**：按原因分类筛选。
- **多选下拉**：品种 / 医院 / 药房 / 随访人（多选=或）。
- **关键词搜索**：匹配 患者 / 电话 / 医生 / 医院 / 药房 / 品种 / 随访人。
- **清空全部**：回到默认状态（本周 + 今天）。
- 所有筛选条件**叠加生效（交集）**。

### 6.4 脱敏与列显隐

筛选区下方：

- **姓名 / 医生 / 电话** 三组「脱敏 ⇄ 不脱敏」开关，**三者相互独立**，实时影响列表与导出（所见即所得）。
- **脱敏方式**（姓名与医生共用）：`保留首字` / `保留首+末字`（默认）/ `仅会员号` / `全部隐藏`。替换几个字符就有几个 `*`。
  - 选「仅会员号」时：患者列显示会员号、医生列显示 `—`、**电话列自动隐藏**。
- **表头显隐**：任意列可隐藏/显示。

### 6.5 名单表

| 列 | 说明 |
|---|---|
| 患者 / 电话 / 医生 / 科室 | 按脱敏开关取值 |
| 品种 / 医院 / 药房 | |
| 最近购药 | 最近一次购药日期 |
| **购药数量** | **最近一次的合计数量**（同日多行相加后） |
| 周期(天) | 该患者该品种实际使用的周期 |
| 应购药日期 | 下次应购日期；「应回已回」行显示预判应购日 |
| 距今天数 | 相对真实今天；逾期标红加粗 |
| 匹配随访时间 / 随访类型 / 随访人 | 来自匹配到的那条随访 |
| 状态 | 主状态（+ 应回购细分） |
| 未购药原因 | **可点选**：自动判定预填，人工点选覆盖 |
| 随访信号 | 正常状态 / 预判延期 / 已脱落（按颜色区分） |
| 跟进备注 | **仅「应回购 / 已逾期」可填写**（其余状态显示 `—`），内容随快照与回传保存 |

- **展开行**（点行）：显示该患者的随访明细（用药状态、是否按时购药、易脱落标记与原因、停药/推迟原因、随访小结）+ 跟进备注。
- **已逾期 + 未到期** 行也能标注未购药原因；无标注时按状态显示占位（已购药 / — / 未标）。

### 6.6 整体小结

按品种分别生成，可切换品种、复制成文本。内容包含：

1. 所选周应回购人数（应回已回 / 应回未回）
2. 未购药原因分类汇总（**父类人数 = 子类之和**）
3. 下周预计复购人数（正常回购 / 推迟，附推迟原因）

- 「**管理分类**」可增删改未购药原因分类树 / 加子类 / 恢复默认，**「未购药原因」列选项与小结文案同步跟随**。
- 小结统计范围**跟随医院 / 药房筛选**，品种由面板内 chips 切换。

---

## 7. 导出与快照

名单区右侧四个按钮：

| 按钮 | 产物 | 说明 |
|---|---|---|
| **导出名单** | `.xlsx` | 表头加粗 + 品牌色底；状态列着色（应回购红 / 已逾期橙 / 未到期蓝，细分：应回未回红、应回已回绿）；逾期天数标红；周内应购行整行浅金底 |
| **导出随访回传表** | `.xlsx` | 见第 8 节 |
| 下载脱敏快照 | 单文件 `.html` | 姓名/电话/医生已脱敏，**不含明文个人信息，可安全外发** |
| 下载不脱敏快照 | 单文件 `.html` | 含明文个人信息，**仅可发给可信接收方**（生成前有风险提示） |

**导出名单遵循"所见即所得"**：内容按当前三组脱敏开关输出。想要全明文名单 → 把姓名/医生/电话都切到「不脱敏」再导出（此时会弹隐私提示）。

### 7.1 快照的品种范围（按需只发某几个品种）

点任意一个「下载快照」按钮后，**先弹出品种勾选弹窗**：

- 默认勾选**当前筛选出的品种**（若当前无品种筛选，则默认全选）。
- 可**全选 / 清空 / 逐个改选**；一个都不勾时「确定」置灰。
- 点「取消」或按 `Esc` 则中止，不下发文件。

**只有被勾选的品种数据会被写入文件**——所以可以把品种 A 的名单单独发给他人，对方拿不到其他品种的任何信息。

导出的文件名会带上品种标识，便于接收方一眼看出范围：

| 范围 | 文件名示例 |
|---|---|
| 单品种 | `预计购药名单_百泽安_脱敏快照.html` |
| 2~3 个品种 | `预计购药名单_百泽安+百悦泽_脱敏快照.html` |
| 4 个及以上 | `预计购药名单_多品种_脱敏快照.html` |

### 7.2 快照内仍可按品种筛选

快照的筛选面板保留，但**品种选项只含该快照内嵌的品种**——接收方能在收到的范围内自由切换查看，
却不会看到被排除在外的品种。取消勾选后也能退回该快照的全量范围。

### 7.3 快照不含脱敏开关与整体小结

- **不显示脱敏切换栏**：打开快照后**沿用生成时的脱敏状态**（生成脱敏版即一直脱敏），
  界面不再提供脱敏相关开关，避免接收方误改后外发。
- **不含「整体小结」面板与小结文本**：小结的统计口径依赖本地销售明细，快照中无法重算；
  为避免接收方按错误口径理解数据，导出的快照直接不包含小结。
- 分类维护面板仍随快照保存（不打开），以保证「未购药原因」列显示中文标签而非内部 key。

**快照其余特性**：单文件、离线可双击打开、只读展示当前筛选后的名单，**手动填写的跟进备注与原因修正会一并保存**。

---

## 8. 随访回传闭环

**解决的问题**：在看板里填的跟进备注、点选的未购药原因，原本关掉页面就没了，下一轮又要重填。

**用法（两步）**

1. 本轮工作完成后，点「**导出随访回传表**」→ 得到 `随访回传_日期.xlsx`。
2. **下一轮**上传数据时，把这个文件**连同销售/随访/周期表一起拖进去** → 系统自动接续。

**接续效果**

| 回传内容 | 下一轮的表现 |
|---|---|
| 跟进备注 | 回到「跟进备注」列，**仍可继续编辑** |
| 未购药原因 | **等同人工点选**（优先于文本自动判定） |

顶部会提示「已接续回传 N 条备注 / M 条原因」。**已在本轮改过的内容不会被导入覆盖。**

### 回传表结构（如需手工造表，照此列名）

| 列名 | 内容 | 说明 |
|---|---|---|
| 任务状态 | 固定 `已完成` | **识别签名**，缺了会认不出 |
| 服务摘要 | 固定 `跟进回传` | **识别签名** + 回流标记 |
| 患者姓名 | 明文 | **匹配键** |
| 联系电话 | 明文 | **匹配键（优先）** |
| 产品 | 品种 | 用于同品种优先匹配 |
| 执行时间 | 导出当天 | 决定匹配到哪一周 |
| 执行人 | 随访人 | |
| 未购药原因 | 中文标签（如 `脱落·经济`） | 导入后等同人工点选 |
| 跟进备注 | 填写内容 | 导入后回填到「跟进备注」列 |
| 随访小结 | 原随访小结 | 保留原记录 |

**为什么必须明文**：随访记录靠电话或姓名匹配到患者，脱敏后（`张*三` / `138****1111`）匹配不上。因此回传表**强制明文，且含明文姓名与电话，只能在团队内部流转**。

**导出范围**：只导出「有填写」的行（填了跟进备注 或 点选过未购药原因），避免把整张名单变成随访记录污染随访池。

---

## 9. 隐私与合规

⚠️ **这是本项目最需要严肃对待的部分。**

**数据不出本机**：所有解析与计算都在浏览器内完成，没有后端服务、没有接口调用、没有埋点。关掉浏览器即无残留（除非你生成了文件）。

**四类产物的敏感度**

| 产物 | 敏感度 | 可否外发 |
|---|---|---|
| 脱敏快照 | 低（无明文个人信息） | ✅ 可外发 |
| 脱敏状态下的名单页 | 低 | 仅限投屏/截图场景，注意别切到不脱敏 |
| **导出名单（含不脱敏字段）** | **高** | ⚠️ 仅限可信接收方 |
| **随访回传表** | **高（强制明文）** | ⚠️ 仅限团队内部 |

**操作建议**

- 投屏、共享屏幕、截图前，先确认三组脱敏开关的状态。
- 导出含明文时系统会弹隐私提示——**不要习惯性点确定**。
- 快照文件可以随意发给同事；回传表不行。
- 本机下载目录里的导出文件记得清理。

---

## 10. 维护与二次开发

### 10.1 构建

```bash
cd repurchase_dashboard
python build.py            # 生成 index.html（Web 部署版，vendor 库外链）
python build.py --single   # 生成 index.single.html（单文件版，库内联，约 2 MB）
```

> **`index.html` 和 `index.single.html` 都是构建产物，不要手动改。**
> 改界面 → `index.template.html`；改逻辑 → `app.js` / `pipeline.js` / `mapping.js`；然后重跑 `build.py`。

### 10.2 常见改动指引

| 需求 | 改法 |
|---|---|
| 支持一个新品种 | `mapping.js` → `PRODUCT_FAMILIES` 加一条（含关键词与标准周期） |
| 新报表的列名和现有关键字对不上 | 对应 `KEYWORD_RULES` 里给该字段**补一个关键词**即可 |
| 判断"停药"的措辞有新说法 | `mapping.js` → `RE_DROPOUT_TXT` / `RE_NONSTD_TXT` / `RE_NORMAL_TXT` |
| 未购药原因要加一类 | 界面「管理分类」直接加（用户级）；要改内置默认则改 `DEFAULT_REASON_TREE` |
| 加一列到名单 | `app.js` → `LIST_COLS` 加 `["key", "列名"]`；`buildRows` 里把值放进 row 对象；导出列宽数组同步 +1 |
| 改默认周期算法 / 容差 | `app.js` → `state.cycleAlgo` / `state.cycleTol` 初值 |

### 10.3 逻辑源文件的职责边界

```
index.template.html   ← HTML 骨架 + 全部 CSS（含 :root 变量）
        │  build.py 内联 ↓
mapping.js   纯常量与纯函数：品种归一、表类型签名、字段关键字、随访信号、原因分类
pipeline.js  纯解析：SheetJS 读表 → 按签名识别类型 → 按关键字映射列 → 归一化记录
app.js       业务：聚合购药记录 → 周期算法 → 状态判定 → 筛选 → 渲染 → 导出/快照/回传
```

`mapping.js` 与 `pipeline.js` 无 DOM 依赖，可在 Node 里直接加载（测试就是这么跑的）。

---

## 11. 测试

```bash
cd repurchase_dashboard
node tests/_test_logic.js        # 状态分类边界
node tests/_test_qtycycle.js     # 购药数量 × 周期、三种周期算法
node tests/_test_weekrange.js    # 周截止日 / 参考日 / 周窗口
node tests/_test_summary.js      # 整体小结与原因分类
node tests/_test_export.js       # 导出内容与样式
node tests/_test_callback.js     # 随访回传闭环（含导出→重新导入的端到端回环）
node tests/_test_desentoggle.js  # 三组脱敏开关独立性
node tests/_test_snapshot.js     # 快照剥离
node tests/_test_snapshot_scope.js    # 快照品种范围 / 去小结 / 隐藏脱敏开关
node tests/_test_snapshot_e2e_dom.js  # 真实 DOM 端到端：生成快照 → 打开快照
# 其余同理，全部跑一遍：
for f in tests/_test_*.js; do node "$f" || echo "FAIL $f"; done
```

**当前状态**：`tests/` 下共 **24 个脚本**（21 个 `_test_*.js` 用例 + 3 个诊断/核验辅助脚本）。其中 **4 个依赖本机样例 Excel**（`_test_detect` / `_test_e2e` / `_test_render` / `_test_snapshot_filter`），缺文件时自动打印"跳过"并以 0 退出；**其余全部通过**。

**诊断辅助脚本**（不属于回归用例，按需手动跑）：

```bash
node tests/_diag_doctor_dept.js   # 核对医院/科室/医生是否严格等于末次购药记录的值，并打印实际选中的列
node tests/_diag_data.js          # 看上传文件被识别成什么表、各列映射到了哪个字段
```

> `_test_snapshot_e2e_dom.js` 需要 `jsdom`（不在本项目内）。若报 `Cannot find module 'jsdom'`，
> 用 `NODE_PATH` 指向已装 jsdom 的 `node_modules` 再跑；脚本内置了本机兜底路径，找不到会明确提示并退出 1。

**改动后的最低要求**：改到判定逻辑，至少跑 `_test_logic` / `_test_qtycycle` / `_test_summary`；改到导出，跑 `_test_export`；改到快照，跑 `_test_snapshot` / `_test_snapshot_scope` / `_test_snapshot_e2e_dom`。

---

## 12. 发布部署

### 12.1 日常发布（有仓库写权限）

> **接手方注意**：下面的命令以**原团队的仓库**为例（仅作流程示意）。
> 请先把仓库地址、账号换成**你自己的**，否则命令会指向原团队仓库。
> 顺带说明：这个交付包里没有 `.git`，也没配置任何远端，**所以你不主动添加远端就推送不到任何地方**。

```bash
cd repurchase_dashboard
python build.py && python build.py --single     # 1. 重建产物

git add -A
git commit -m "……"
git push origin main                              # 2. 推源码分支

# 3. 同步单文件版到 gh-pages（Pages 的发布源）
git worktree add .ghp-tmp gh-pages
cp index.single.html .ghp-tmp/index.html
cd .ghp-tmp && git add -A && git commit -m "同步单文件版（……）"
cd .. && git worktree remove .ghp-tmp --force
git push origin gh-pages                          # 4. 推送发布分支
```

推送后 GitHub Pages 通常会自动重建。若没触发，手动触发一次：

```bash
TOKEN=$(printf "protocol=https\nhost=github.com\n\n" | git credential fill | grep "^password=" | cut -d= -f2)
curl -X POST -H "Authorization: token $TOKEN" \
  https://api.github.com/repos/Lynx-Fanlv/reperchace_dashboard/pages/builds
# 轮询确认：GET .../pages/builds/latest 直到 status=built
```

**验证发布**：`curl -sL https://lynx-fanlv.github.io/reperchace_dashboard/ | grep -c 'id="exportBtn"'`，或直接强刷（Ctrl+F5）看界面。

### 12.2 自己部署一份（无仓库权限时）

1. Fork 仓库，或新建仓库后把整个目录推上去。
2. 仓库 **Settings → Pages → Source = Deploy from a branch**，选择 **`gh-pages` 分支 / `(root)`**。
   （若你的仓库还没有 `gh-pages` 分支，可先把 `index.single.html` 改名为 `index.html` 推到 `main`，并把 Source 指向 `main` / `(root)`。）
3. 访问 `https://<你的用户名>.github.io/<仓库名>/`。

### 12.3 零部署方式

直接把 `index.single.html` 发给同事，**双击即用、离线可用**，不需要任何服务器。

---

## 13. 已知限制与注意事项

| 项 | 说明 |
|---|---|
| **同一天重复行会被合并相加** | 若销售明细里存在完全重复的记录（同一笔交易写了两遍），数量会被算成两倍、覆盖天数翻倍。发现某患者覆盖天数明显偏长时，先查原始行是否重复 |
| **周期表按姓名匹配** | 患者用药周期表以**姓名**为键。同名患者会互相覆盖；患者改名后匹配不到 |
| **电话优先、姓名兜底** | 销售表缺电话时退化为按姓名匹配，同名不同人会串数据 |
| **预判应购日需 ≥2 条记录** | 只有 1 条购药记录时无参照基准，该列显示 `—`，不呈现提前/延后 |
| **提前购药一律算「已回」** | 「所选周有购药即应回已回」是有意设计（购药事实优先），提前囤药也会计入已回购 |
| **品种家族靠关键词** | 商品名不含已知关键词的新药会被原样当作品种（不会丢数据，但周期需手工设） |
| **大文件性能** | 纯前端解析，数万行量级没问题；超大文件（十万行+）首次解析会明显卡顿 |
| **快照是只读的** | 快照内不能继续编辑或重新分析 |
| **回传表必须明文** | 见第 8 节，这决定了它只能内部流转 |

---

## 14. 排错 FAQ

**Q：上传后提示「未识别」怎么办？**
逐个检查该文件的表头是否含足够的关键字：销售表需含 `销售时间` + `会员姓名`；随访表需含 `任务状态` + `服务摘要`。缺了就往 `mapping.js` 的 `TABLE_SIGNATURES` / `KEYWORD_RULES` 里补关键词。

**Q：列表是空的 / 一个人都没有？**
先看顶部 `销售明细 N 条`。N=0 说明销售表没被识别（表头问题）。N>0 但列表空，通常是筛选条件叠加太严——点「清空全部」重来。

**Q：某患者的状态看起来不对（明明买了却显示逾期）？**
按顺序查：
1. 该患者的**电话**在销售表和随访表里是否一致（电话不一致会退化成按姓名匹配）；
2. **周期**取值是否正确（周期表 / 品种标准周期）；
3. 「**按数量**」是否该品种勾上了而**购药数量**列是空的；
4. **周期算法**是否与预期一致（重置 vs 累计会差出很多天）；
5. **参考日 / 周截止日**是否被改过。

**Q：改了代码但页面没变化？**
忘了跑 `python build.py`。`index.html` 是产物，直接改源码不生效。

**Q：导出的 Excel 列不对 / 少了列？**
先检查「表头显隐」是否隐藏了列——导出跟随当前可见列（「仅会员号」模式下电话列也会被去掉）。

**Q：`push` 报连接 github.com 失败？**
本机对 `github.com:443` 存在间歇性不通（但 `api.github.com` 与 Pages 站点正常）。**不要放弃**，间隔 15 秒重试 8~10 次通常就能成功。

**Q：`git revert` / `checkout` 之后文件莫名消失？**
本机有文件删除拦截层，曾在 `git revert` 后误删整个 `tests/` 目录。**这类 git 操作后务必 `ls` 关键目录**；若真丢了，`git restore --worktree <目录>/` 可全部找回（内容在 git 里没丢）。

---

## 附录 A：字段映射关键字全表

列标题**包含**下列任一关键词即命中（不区分大小写、忽略空白）。同一字段命中多列时取命中数最多的那一列。

### 销售明细

| 字段 | 关键词 |
|---|---|
| sales_time | 销售时间 / 购药时间 / 成交时间 |
| order_status | 订单状态 |
| product | 商品名称 / 药品名称 / 产品名称 / 产品 / 药品 |
| qty | 销售数量 / 数量 |
| amount | 含税金额 / 无税金额 / 实收金额 / 金额 |
| member_id | 会员号 / 会员编号 / 会员卡号 |
| patient_name | 会员姓名 / 客户姓名 / 患者姓名 / 姓名 |
| phone | 会员电话 / 手机号 / 联系电话 / 手机 / 电话 |
| hospital | **医疗单位**（精确匹配，且只此一列） |
| pharmacy | 药房名称 / 门店 / 药店名称 / 药店 / 药房 |
| physician | 处方医生 / 开单医生 / 医生 |
| department | 处方科室 / 科室 / 就诊科室 |
| indication | 适应症 / 临床诊断 / 诊断 |
| age / gender | 年龄 / 性别 |

### 随访任务（单值字段）

| 字段 | 关键词 |
|---|---|
| task_status | 任务状态 |
| f_patient_name | 患者 / 患者姓名 / 姓名 |
| is_key | 是否重点患者 / 重点患者 |
| patient_id | 患者编号 / 患者编码 / 编号 |
| f_phone | 联系电话 / 手机号 / 电话 / 电话号码 |
| plan_time | 计划执行时间 / 计划执行日期 |
| f_product | 产品 / 药品 / 商品 |
| f_indication | 适应症 |
| task_no | 任务编号 / 任务ID |
| summary_type | 服务摘要 / 任务摘要 / 随访类型 / 任务类型 |
| create_time | 创建时间 |
| executor | 执行人 / 随访人 / 经办人 / 负责人 / 坐席 / 话务员 / 客服 |
| exec_time | 执行时间（排除含"计划"的列） |
| cancel_reason | 取消原因 |
| reason_label | 未购药原因 / 未购药原因分类（回传表专用） |
| callback_note | 跟进备注（回传表专用） |

### 随访任务（多值字段：跨列取有值者）

| 字段 | 关键词 |
|---|---|
| usage_status | 患者目前的用药状态 |
| purchased_on_time | 是否已按时购药 / 已按时购药 / 用药周期状态 |
| is_dropout | 是否判定患者属于易脱落 / 是否脱落 / 是否确认脱落 |
| dropout_reason | 判断为易脱落的原因 / 脱落原因 / 脱落/流失原因 |
| still_using | 还在按照处方继续使用 |
| near_usage | 患者近一次用药情况 |
| delay_reason | 推迟用药的原因 / 延迟用药的具体原因 / 未按计划持续用药原因 |
| stop_reason | 停药的原因是什么 / 停药的具体原因 / 停止用药，主要是谁的建议 |
| dosage | 的用法用量 / 当前用法用量 |
| follow_note | 随访小结 / 小结 / 随访记录 / 患者反馈内容 / 患者反馈 / 备注 |

### 患者用药周期表

| 字段 | 关键词 |
|---|---|
| c_patient_name | 患者 / 患者姓名 / 姓名 |
| cycle_days | 用药周期 / 周期天数 / 购药周期 / 周期 |

### 表类型识别签名

| 类型 | 签名关键字 |
|---|---|
| 销售明细 | 销售时间 + 会员姓名（强规则）；签名：销售时间 / 商品名称 / 会员姓名 / 药房名称 |
| 随访任务 | **任务状态 + 服务摘要（强规则）**；签名：任务状态 / 服务摘要 / 联系电话 / 计划执行时间 |
| 用药周期 | 用药周期 |

---

## 附录 B：术语表

| 术语 | 含义 |
|---|---|
| **参考日** | 时间基准日期，默认今天。改它即可回看任意历史周 |
| **周截止日** | 定义"本周"在哪天结束。默认周日 = 自然周 |
| **所选周** | 状态判定的时间窗口（上周/本周/下周 之一） |
| **单次用药周期** | 一次用药覆盖的天数，来自周期表或品种标准周期 |
| **购药数量** | 销售明细的「销售数量」，一次购买几个单位（配合「按数量」开关使用） |
| **覆盖天数** | 本次购药能撑多少天 = 数量 × 周期 |
| **库存耗尽日** | 按周期算法推进得到的"药吃完了"的日期 |
| **应购药日期** | 下次应该买的日期 = 全部购药记录推进后的库存耗尽日 |
| **预判应购日** | 上一次本该买的日期（用于算提前/延后），需 ≥2 条购药记录 |
| **应回未回** | 应购日落在所选周、当周没买 |
| **应回已回** | 所选周内有购药记录（含提前购药） |
| **随访信号** | 从匹配到的随访内容判出的三类：正常状态 / 预判延期 / 已脱落 |
| **未购药原因** | 可人工点选的分类树（推迟购药 / 延长周期 / 脱落 / 转渠道 / 随访失败未探寻原因） |
| **回传表** | 把看板填写内容导成随访格式，供下一轮自动接续 |

---

*文档随代码演进。修改核心口径时请同步更新本文件与 `README.md`。*
